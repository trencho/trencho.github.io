# Dependabot — opens weekly update PRs. Patch/minor are grouped (and auto-merged by
# dependabot-auto-merge.yml once CI is green); majors land in their own PR for review.
# Deliberately-held majors are ignored so they don't reopen every week (Dependabot applies
# `ignore` retroactively and auto-closes any offending open PR).
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "04:00"
      timezone: "Europe/Skopje"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # @types/node tracks the RUNTIME major, always -- typings for a Node the project does
      # not run describe APIs that are not there. It had reached 26.2.0 while every runtime
      # pin here says 24. A runtime-image hold cannot reach an npm package, so without this
      # one the types walk to the next major alone; both lift on the same condition.
      # lift-when: eol-lts nodejs 26
      - dependency-name: "@types/node"
        versions: [">=25"]
      # ESLint 10 breaks eslint-plugin-react (removed context.getFilename()) — held until the
      # plugin ships an ESLint-10-compatible release.
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
        # lift-when: npm-peer eslint-plugin-react allows eslint >=10
      - dependency-name: "@eslint/js"
        update-types: ["version-update:semver-major"]
        # lift-when: npm-peer eslint-plugin-react allows eslint >=10
      # TypeScript 7 (native compiler) is not yet supported by typescript-eslint (peer-capped
      # <6.1.0) and breaks Yarn's TS compat patch — held until the toolchain catches up.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
        # lift-when: npm-peer typescript-eslint allows typescript >=7

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "05:00"
      timezone: "Europe/Skopje"
    commit-message:
      prefix: "ci"
